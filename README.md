# landingudak

iPhone 18 사전예약 랜딩 페이지. 빌드 과정 없는 순수 정적 사이트 (udak 레포와 동일한 방식).

- `index.html` — 페이지 전체 (인라인 CSS/JS)
- `assets/` — 이미지
- `_headers` — Cloudflare Pages 응답 헤더 (CSP 등)

## 배포
Cloudflare Pages에 이 레포를 연결. 빌드 커맨드 없음 (Build output directory: `/`).
